name: Update Game Data
on:
  schedule:
    - cron: '30 12 * * *'  # 每天UTC 12:30（北京时间20:30）
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest  # 使用通用标签确保兼容性
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq  # 安装jq工具

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install requests python-dateutil

      - name: Create data directory
        run: mkdir -p data

      - name: Run scraper
        run: python Src/scraper.py

      - name: Commit changes
        uses: EndBug/add-and-commit@v9
        with:
          add: 'data/*.json'
          commit_message: 'Auto-update game data'
          push: true

      - name: Upload debug info
        if: failure()
        uses: actions/upload-artifact@v4  # 使用v4版本
        with:
          name: debug-info
          path: |
            data/*.json
            Src/scraper.py
